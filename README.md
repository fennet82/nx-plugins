# nx-graphify

Nx workspace containing the `nx-graphify` plugin.

See [docs/superpowers/specs](./docs/superpowers/specs) for the design spec.
The plugin source lives under `packages/nx-graphify`.

## Development

```bash
pnpm install
npx nx build nx-graphify
npx nx test nx-graphify
```
